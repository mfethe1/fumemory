# Issue Tracker

This repo tracks work in GitHub Issues for `mfethe1/fumemory`.

Use `gh issue create` or the GitHub API to create issues. New implementation issues should receive the `needs-triage` label so they enter the normal triage flow.

Issue bodies should use the repo domain language from `CONTEXT.md` and reference relevant ADRs in `docs/adr/`.
