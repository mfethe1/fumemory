# Triage Labels

Canonical triage label mapping:

- `needs-triage` — maintainer needs to evaluate
- `needs-info` — waiting on reporter
- `ready-for-agent` — fully specified and ready for an AFK agent
- `ready-for-human` — needs human implementation or judgment
- `wontfix` — will not be actioned

If a label does not exist in GitHub, create it before publishing issues that require it.
