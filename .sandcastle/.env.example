# Required when using the default Claude Code Sandcastle runner.
ANTHROPIC_API_KEY=

# Optional. The repo-local runner fetches issue bodies through the host gh CLI,
# so GH_TOKEN is only needed if a future prompt needs gh inside the sandbox.
GH_TOKEN=
